apiVersion: v1
kind: Secret
metadata:
  name: db-secret
type: Opaque
stringData:
  # In a real environment, this is injected by Vault.
  # For this demo, replace with your actual RDS endpoint and password.
  url: "postgres://postgres:<DB_PASSWORD>@<RDS_ENDPOINT>:5432/commerce"
